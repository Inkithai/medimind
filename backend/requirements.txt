# MediMind — Python dependencies for deployment
# ──────────────────────────────────────────────
# chromadb is used for TWO things:
#   1. Local vector storage  → NOT needed when VECTOR_STORE=supabase
#   2. Local ONNX embeddings → NEEDED when OPENAI_API_KEY is not set
#
# Since we use Supabase for storage but rely on chromadb's built-in
# ONNX MiniLM model for generating embeddings (free, no API key needed),
# chromadb must be installed. If you set OPENAI_API_KEY for embeddings
# instead, you can remove chromadb to save ~200MB install size + ~50MB RAM.

openai
pdfplumber
pymupdf
pillow
chromadb
python-dotenv
python-dateutil
fastapi
uvicorn[standard]
python-multipart
# Structured JSON logs (api.py) + /metrics endpoint — imported at startup,
# must be present or the service crashes on boot
python-json-logger
prometheus-client
supabase
cloudinary
pyjwt
# Optional WHO antidote reference knowledge graph (fail-open when not
# configured — see graph_db.py / poisoning_kg.py). Pure-Python driver.
neo4j
